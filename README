cl-shell provides a processing model inspired by the Unix shell
pipeline facility. It replaces the shell programming concept of filters
with lambda expressions  and channels. Like the shell pipeline facility,
cl-shell is  a facility for programming with multiple, concurrent threads.
